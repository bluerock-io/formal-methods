{
  "name": "webpage",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo 'no tests specified'; exit 1",
    "format": "prettier --write .",
    "build": "npx @11ty/eleventy build --pathprefix=formal-methods",
    "start": "npx @11ty/eleventy --serve --pathprefix=formal-methods",
    "watch": "npx @11ty/eleventy --serve --watch --pathprefix=formal-methods",
    "dev": "npx @11ty/eleventy --serve --watch --pathprefix=formal-methods"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@11ty/eleventy": "^3.1.1"
  }
}
